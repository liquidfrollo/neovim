### Frollo's fixed ThePrimeagen's init.lua
Updated packages including Harpoon2, treesitter, and others for current working versions

run ./dev to move config files

update install packages with :TSUpdate :Lazy sync
make sure node and npm are installed so that mason can download what it needs to work



